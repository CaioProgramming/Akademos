import { ReactiveGlowDirective } from './reactive-glow.directive';

describe('ReactiveGlowDirective', () => {
  it('should create an instance', () => {
    const directive = new ReactiveGlowDirective();
    expect(directive).toBeTruthy();
  });
});
